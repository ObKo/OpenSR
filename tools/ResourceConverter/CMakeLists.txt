INCLUDE_DIRECTORIES(
    ${CMAKE_SOURCE_DIR}/libRanger
)

SET(RESOURCE_CONVERTER_SRCS
    main.cpp
    pkg2rpkg.cpp
)

ADD_EXECUTABLE(rangersresconv ${RESOURCE_CONVERTER_SRCS})
TARGET_LINK_LIBRARIES(rangersresconv ranger)