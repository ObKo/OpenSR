$#include <Types.h>
$#include <Texture.h>
$#include <AnimatedTexture.h>
$pfile "Texture.pkg"

namespace Rangers
{
class AnimatedTexture: public Texture
{
public:
    GLuint openGLTexture(int i) const;

    int seek() const;
    int size() const;
    int count() const;
};
};

#endif
